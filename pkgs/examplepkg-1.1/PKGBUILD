pkgname=examplepkg
pkgver=1.1
pkgrel=1
pkgdesc='Marker package: /etc/examplepkg says which ring built it (rc)'
arch=('any')
license=('MIT')
options=('!debug')
package() {
  install -Dm644 /dev/stdin "$pkgdir/etc/examplepkg" <<< "rc"
}
